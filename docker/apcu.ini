; APCu is compiled in via install-php-extensions, but the extension
; ships disabled by default. Explicitly enable it so Drupal's
; chainedfast cache backend (see web/sites/default/settings.platform.php)
; has a real in-process cache to write to.
apc.enabled=1
apc.enable_cli=1
apc.shm_size=64M
