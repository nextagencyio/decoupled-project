# Nested under Content (system.admin_content) rather than top-level
# admin. Drupal surfaces this on /admin/content as a sibling of the
# main node list, which is semantically correct — this IS a content
# tool, not a site-wide config page.
auditcrawl.report:
  title: 'AuditCrawl'
  description: 'SEO Content Strategy — create draft posts from your report.'
  route_name: auditcrawl.report
  parent: system.admin_content
  weight: 10

auditcrawl.connect:
  title: 'Connect'
  parent: auditcrawl.report
  route_name: auditcrawl.connect
  weight: 10

auditcrawl.schedule:
  title: 'Schedule (premium)'
  parent: auditcrawl.report
  route_name: auditcrawl.schedule
  weight: 20
